# 复制为 `.env` 后按本地环境修改；不要提交真实 `.env`。

# 使用 MySQL 持久化活动场景、模拟历史、预测和规划记录。
# GREEN_AGENT_DATABASE_URL=mysql+pymysql://root:135790@127.0.0.1:3306/green_agent?charset=utf8mb4

# 未设置 GREEN_AGENT_DATABASE_URL 时，可指定 SQLite 文件路径。
# GREEN_AGENT_DB_PATH=/tmp/green-agent-demo.sqlite
