## 1. 样式盘点

- [x] 1.1 盘点 `frontend/src/styles.css` 中全局背景、面板、表单、按钮、卡片、历史页和 AI 助手相关选择器。
- [x] 1.2 识别 `frontend/src/App.tsx` 中直接写死且可能影响亮色可读性的地图颜色。

## 2. 亮色主题实现

- [x] 2.1 将全局页面背景、顶部状态区、三栏面板和历史页容器调整为白色/浅绿亮色视觉。
- [x] 2.2 将输入框、选择框、文本域、主按钮、次按钮和禁用态调整为亮色可读样式。
- [x] 2.3 将指标卡片、编辑卡片、历史记录卡片、路线卡片、路线任务序列和选中态调整为亮色可读样式。
- [x] 2.4 将错误、警告、空状态和节点放置提示调整为亮色主题下可区分的状态样式。
- [x] 2.5 将 AI 助手入口、弹层、消息气泡、Markdown 内容、代码块、错误提示和发送表单调整为亮色主题。
- [x] 2.6 必要时调整地图容器背景、边框、阴影和基础图边颜色，确保亮色工作台中地图元素清晰。

## 3. 范围保护

- [x] 3.1 确认没有改变首页三栏布局、历史页结构、AI 助手结构和现有控件文案。
- [x] 3.2 确认没有新增依赖、主题切换机制、API 调用或业务逻辑变更。

## 4. 验证

- [x] 4.1 运行前端测试，确认现有交互行为未回归。
- [x] 4.2 运行前端构建，确认 TypeScript 和 Vite 构建通过。
- [x] 4.3 手动检查首页、历史页、路线选中状态、地图基础图边、提示状态和 AI 助手弹层的亮色显示。
