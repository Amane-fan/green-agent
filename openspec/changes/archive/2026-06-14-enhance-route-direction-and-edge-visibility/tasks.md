## 1. 地图线条样式

- [x] 1.1 为普通基础图边定义更清晰的渲染样式，提升颜色、线宽和透明度在瓦片背景下的可见性
- [x] 1.2 为选中路线段定义流动虚线样式，使用 `stroke-dasharray` 和 `stroke-dashoffset` 动画表达行驶方向
- [x] 1.3 为流动虚线添加 `prefers-reduced-motion` 降级样式，降低动画干扰

## 2. 前端渲染接线

- [x] 2.1 更新基础图边 `Polyline` 的 `pathOptions`，应用增强后的基础图边样式，并确保不带方向样式 class
- [x] 2.2 更新选中路线段 `Polyline` 的 `pathOptions`，应用流动虚线方向样式，并保留路线颜色、路段弹窗和路段级渲染
- [x] 2.3 确认选中路线段坐标顺序继续遵循 `route.path_node_ids` 的相邻节点顺序，保证虚线流动方向与路线方向一致

## 3. 测试覆盖

- [x] 3.1 更新 React Leaflet 测试 mock 或断言，覆盖基础图边增强后的颜色、线宽、透明度和非方向 class
- [x] 3.2 添加或更新测试，确认选中路线段带有流动虚线方向 class，并且未选中路线不渲染方向覆盖层
- [x] 3.3 保持现有路线选择、路段指标弹窗、车辆起点和回收点高亮测试通过

## 4. 验证

- [x] 4.1 运行前端测试：`npm test`
- [x] 4.2 运行前端构建：`npm run build`
