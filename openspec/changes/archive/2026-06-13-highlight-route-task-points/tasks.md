## 1. 测试准备

- [x] 1.1 扩展前端测试规划 payload，使选中路线的 `path_node_ids` 包含一个不在 `route.stops` 中的途经垃圾桶
- [x] 1.2 添加右侧路线卡片断言，覆盖车辆起点、按序回收垃圾桶、目标处理厂展示，并确认途经垃圾桶不进入任务序列
- [x] 1.3 添加地图 marker 断言，覆盖选中路线车辆起点和回收垃圾桶获得高亮样式，途经垃圾桶保持普通样式

## 2. 前端派生逻辑

- [x] 2.1 在 `frontend/src/App.tsx` 中从 `selectedRoute.vehicle_id` 和 `scenario.vehicles` 派生当前选中路线车辆起点节点
- [x] 2.2 从 `selectedRoute.stops` 派生当前选中路线回收垃圾桶集合和有序任务序列，确保不使用 `path_node_ids` 判断任务点
- [x] 2.3 在切换选中路线、重新规划、推进时间或重新生成场景时，确保起点和回收点强调随 `selectedRoute` 同步更新

## 3. 右侧路线面板

- [x] 3.1 将路线卡片中的单行 `node_id -> node_id` 文本替换为结构化任务链
- [x] 3.2 在任务链中显示车辆起点、回收垃圾桶顺序号和满溢率、目标处理设施
- [x] 3.3 保持路线卡片选择行为、颜色标识、距离指标和无障碍状态不退化

## 4. 地图重点展示

- [x] 4.1 为选中路线车辆起点添加高亮 marker 样式或叠加标识
- [x] 4.2 为选中路线回收垃圾桶添加路线色外环、顺序编号或等效高亮标识
- [x] 4.3 保持仅途经垃圾桶的普通 marker 样式，并保留基础 Popup 信息
- [x] 4.4 确保未选中路线不渲染起点和回收点高亮

## 5. 验证

- [x] 5.1 运行前端测试，确认新增和既有路线可视化测试通过
- [x] 5.2 运行 OpenSpec 校验或状态检查，确认 `highlight-route-task-points` 已达到可实施状态
