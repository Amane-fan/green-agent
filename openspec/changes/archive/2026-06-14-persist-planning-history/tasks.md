## 1. 后端模型与存储测试

- [x] 1.1 为规划记录摘要、详情、恢复响应和 `PlanningResult.record_id` 添加后端测试期望
- [x] 1.2 为 `KnowledgePool` 增加规划记录保存、列表、详情读取和恢复相关单元测试
- [x] 1.3 覆盖失败规划不创建记录、恢复不存在记录返回 `404`、场景 reset 后历史记录仍可查询的测试场景

## 2. 数据库配置与持久化层

- [x] 2.1 添加数据库连接配置，支持 `GREEN_AGENT_DATABASE_URL`，并保留本地/测试 SQLite fallback
- [x] 2.2 引入并配置 MySQL 所需依赖，同时保证测试不依赖外部 MySQL 服务
- [x] 2.3 扩展存储初始化逻辑，创建 `planning_records` 表及必要索引
- [x] 2.4 实现规划记录 JSON 快照序列化、反序列化和摘要查询逻辑
- [x] 2.5 调整 `reset` 语义，确保重置活动场景不删除规划历史记录

## 3. 后端 API

- [x] 3.1 扩展后端 Pydantic 模型，定义规划记录摘要、详情和恢复响应结构
- [x] 3.2 在 `POST /api/planning/routes` 成功规划后保存记录，并在响应中返回 `record_id`
- [x] 3.3 新增 `GET /api/planning-records`，按创建时间倒序返回规划记录摘要列表
- [x] 3.4 新增 `GET /api/planning-records/{id}`，返回单条记录的场景快照和规划结果
- [x] 3.5 新增 `POST /api/planning-records/{id}/restore`，将记录场景快照设置为当前活动场景并返回 `scenario` 和 `plan`
- [x] 3.6 运行后端测试，确认现有规划、场景、仿真和新增历史记录测试通过

## 4. 前端类型、API 与测试

- [x] 4.1 添加前端规划记录摘要、详情和恢复响应 TypeScript 类型
- [x] 4.2 添加规划历史列表、详情读取和恢复 API 调用函数
- [x] 4.3 扩展前端测试 mock，覆盖新规划后刷新历史列表和返回 `record_id`
- [x] 4.4 添加历史记录恢复测试，确认恢复后地图节点、图边、路线卡片和默认选中路线使用记录快照
- [x] 4.5 添加恢复后继续编辑或推进时间会清空旧规划结果的测试

## 5. 前端交互实现

- [x] 5.1 在页面中增加规划历史记录列表，展示场景名称、模拟时间、路线数量、总距离、油耗、碳排放和创建时间
- [x] 5.2 新规划成功后刷新历史记录列表，并标记或可见新创建记录
- [x] 5.3 实现点击历史记录调用 restore API，并更新 `scenario`、`plan`、错误状态和待放置状态
- [x] 5.4 复用现有选中路线逻辑，确保恢复记录后默认高亮第一条路线并支持切换路线
- [x] 5.5 确保恢复后添加节点、添加边、随机生成场景和推进时间都会清空旧 `plan` 与选中路线

## 6. 文档与验证

- [x] 6.1 更新 README 或 `docs/demo-workflow.md`，加入 MySQL Docker 创建命令和 `GREEN_AGENT_DATABASE_URL` 示例
- [x] 6.2 更新演示步骤，说明规划历史记录、恢复记录和基于恢复场景继续规划的流程
- [x] 6.3 运行前端测试，确认历史记录交互和既有地图路线测试通过
- [x] 6.4 运行 OpenSpec 校验或状态检查，确认 `persist-planning-history` 达到可实施状态
