## 1. 测试覆盖

- [x] 1.1 更新 React Leaflet 测试 mock，使其暴露 TileLayer 渲染、地图点击事件、Polyline 弹窗内容、路线卡片选择状态和 Polyline path options。
- [x] 1.2 添加前端测试，验证规划结果默认选中第一条路线，并且只将该路线渲染为高亮路线覆盖层。
- [x] 1.3 添加前端测试，验证选择另一张路线卡片会更新选中卡片，并切换地图上的高亮路线。
- [x] 1.4 添加前端测试，验证基础图边弹窗只显示长度，而高亮的选中路线段弹窗显示长度、车辆燃油率、路段燃油和路段碳排放。
- [x] 1.5 添加前端测试，验证自定义节点创建会进入待放置模式，并在点击的地图坐标处创建节点。

## 2. 路线选择状态

- [x] 2.1 在 React app 中添加选中路线状态，并在每次加载新规划结果时重置为第一条已规划路线。
- [x] 2.2 将右侧路线卡片转换为可访问的可选择控件，并通过视觉状态标识当前选中路线。
- [x] 2.3 过滤路线覆盖层渲染，使地图上只高亮选中路线。

## 3. 地图图层和路段指标

- [x] 3.1 恢复 React Leaflet TileLayer 背景，并将其置于图边、标记和路线覆盖层下方。
- [x] 3.2 将选中路线渲染拆分为可点击的逐路段 Polylines，同时保留重叠路线几何的弯曲渲染。
- [x] 3.3 从匹配的图边权重推导选中路线段长度，并从所选车辆燃油率推导路段燃油。
- [x] 3.4 添加路线段弹窗，展示路段长度、所选车辆燃油率、预计路段燃油和预计路段碳排放。
- [x] 3.5 保持基础图边弹窗仅显示路线长度。

## 4. 基于地图的自定义节点放置

- [x] 4.1 将立即创建自定义节点的按钮替换为垃圾桶、车辆和处理设施的待放置模式。
- [x] 4.2 添加 React Leaflet 地图点击处理器，在点击的纬度和经度处创建待放置节点类型。
- [x] 4.3 创建节点后清除待放置状态，并在场景编辑后清除过期规划结果。
- [x] 4.4 当存在待放置的自定义节点类型时，显示简洁的放置提示。

## 5. 验证

- [x] 5.1 运行聚焦的前端测试套件，确认新的路线选择、路段弹窗、瓦片图层和地图放置测试通过。
- [x] 5.2 运行完整前端构建，确认 TypeScript 和 Vite 生产打包通过。
- [x] 5.3 根据 OpenSpec delta requirements 手动复核更新后的路线和地图交互。
